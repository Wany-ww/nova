<Window x:Class="FlowEngine.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:local="clr-namespace:FlowEngine"
        mc:Ignorable="d"
        Title="NOVA" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#11111b"
        Icon="icon.ico">
    
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32" 
                      ResizeBorderThickness="5" 
                      GlassFrameThickness="1"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#313244"/>
                                <Setter Property="Foreground" Value="#cdd6f4"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#45475a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#f38ba8"/>
                                <Setter Property="Foreground" Value="#11111b"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#e78284"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style TargetType="TabControl">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition x:Name="HeaderRow" Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <!-- Tab Header Area: Background is transparent to show the parent's panel background -->
                            <Border Grid.Row="0" Background="Transparent" Panel.ZIndex="1" Padding="0">
                                <TabPanel IsItemsHost="True" x:Name="HeaderPanel" Margin="0"/>
                            </Border>
                            <Border Grid.Row="1" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter ContentSource="SelectedContent"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="HeaderRow" Property="Height" Value="0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="border" Background="Transparent" BorderThickness="0" Margin="0" Padding="0">
                            <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Custom Title Bar -->
        <Border Grid.Row="0" Name="TitleBarBorder" Background="#1e1e2e" BorderBrush="#313244" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Icon and Title -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
                    <Image Source="icon.ico" Width="16" Height="16" Margin="0,0,8,0"/>
                    <TextBlock Name="TitleTextBlock" Text="NOVA" Foreground="#cdd6f4" FontWeight="Bold" FontSize="12" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Spacer (draggable region) -->
                <Grid Grid.Column="1" Background="Transparent" WindowChrome.IsHitTestVisibleInChrome="False"/>

                <!-- Window Buttons -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" WindowChrome.IsHitTestVisibleInChrome="True">
                    <Button Content="—" Width="46" Height="31" Click="MinimizeButton_Click" Background="Transparent" Foreground="#a6adc8" BorderThickness="0" Style="{StaticResource TitleBarButtonStyle}"/>
                    <Button Content="🗖" Width="46" Height="31" Click="MaximizeButton_Click" Background="Transparent" Foreground="#a6adc8" BorderThickness="0" Style="{StaticResource TitleBarButtonStyle}"/>
                    <Button Content="✕" Width="46" Height="31" Click="CloseButton_Click" Background="Transparent" Foreground="#a6adc8" BorderThickness="0" Style="{StaticResource CloseButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content (WebView & Docking Panels) -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="LeftDockCol" Width="0"/> <!-- Left Dock -->
                <ColumnDefinition x:Name="LeftSplitterCol" Width="Auto"/> <!-- Left Splitter -->
                <ColumnDefinition Width="*"/>    <!-- Central Content Area -->
                <ColumnDefinition x:Name="RightSplitterCol" Width="Auto"/> <!-- Right Splitter -->
                <ColumnDefinition x:Name="RightDockCol" Width="0"/> <!-- Right Dock -->
            </Grid.ColumnDefinitions>

            <!-- Left Dock Panel -->
            <Border Grid.Column="0" x:Name="LeftDockPanelBorder" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#11111b" BorderBrush="#313244" BorderThickness="0" Visibility="Collapsed">
                <TabControl x:Name="LeftDockTabControl" Background="Transparent" BorderThickness="0" Margin="0" Padding="0">
                </TabControl>
            </Border>
            <!-- Left Splitter -->
            <GridSplitter Grid.Column="1" Width="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#313244" x:Name="LeftDockSplitter" Visibility="Collapsed"/>

            <!-- Right Splitter -->
            <GridSplitter Grid.Column="3" Width="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#313244" x:Name="RightDockSplitter" Visibility="Collapsed"/>
            <!-- Right Dock Panel -->
            <Border Grid.Column="4" x:Name="RightDockPanelBorder" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#11111b" BorderBrush="#313244" BorderThickness="0" Visibility="Collapsed">
                <TabControl x:Name="RightDockTabControl" Background="Transparent" BorderThickness="0" Margin="0" Padding="0">
                </TabControl>
            </Border>

            <!-- Central Content Area (Top Dock, WebView, Bottom Dock) -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition x:Name="TopDockCol" Height="0"/> <!-- Top Dock -->
                    <RowDefinition x:Name="TopSplitterCol" Height="Auto"/> <!-- Top Splitter -->
                    <RowDefinition Height="*"/>    <!-- WebView -->
                    <RowDefinition x:Name="BottomSplitterCol" Height="Auto"/> <!-- Bottom Splitter -->
                    <RowDefinition x:Name="BottomDockCol" Height="0"/> <!-- Bottom Dock -->
                </Grid.RowDefinitions>

                <!-- Top Dock Panel -->
                <Border Grid.Row="0" x:Name="TopDockPanelBorder" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#11111b" BorderBrush="#313244" BorderThickness="0" Visibility="Collapsed">
                    <TabControl x:Name="TopDockTabControl" Background="Transparent" BorderThickness="0" Margin="0" Padding="0">
                    </TabControl>
                </Border>
                <!-- Top Splitter -->
                <GridSplitter Grid.Row="1" Height="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#313244" x:Name="TopDockSplitter" Visibility="Collapsed"/>

                <!-- WebView -->
                <wv2:WebView2 Grid.Row="2" x:Name="webView" />

                <!-- Bottom Splitter -->
                <GridSplitter Grid.Row="3" Height="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#313244" x:Name="BottomDockSplitter" Visibility="Collapsed"/>
                <!-- Bottom Dock Panel -->
                <Border Grid.Row="4" x:Name="BottomDockPanelBorder" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#11111b" BorderBrush="#313244" BorderThickness="0" Visibility="Collapsed">
                    <TabControl x:Name="BottomDockTabControl" Background="Transparent" BorderThickness="0" Margin="0" Padding="0">
                    </TabControl>
                </Border>
            </Grid>

        </Grid>
    </Grid>
</Window>
